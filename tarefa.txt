CREATE TABLE IF NOT EXISTS `Tarefas` (
    `id` INTEGER PRIMARY KEY AUTOINCREMENT, 
    `Tarefa` VARCHAR(255) NOT NULL UNIQUE,
    `Horario` VARCHAR(255) NOT NULL UNIQUE, 
    `createdAt` DATETIME NOT NULL, 
    `updatedAt` DATETIME NOT NULL
    );